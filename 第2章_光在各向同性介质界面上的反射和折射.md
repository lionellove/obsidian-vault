# 第 2 章 光在各向同性介质界面上的反射和折射```

## 2.1 概述
```table-of-contents
```

当光波从一种介质传播到另一种介质时，在分界面上会发生反射和折射现象。麦克斯韦电磁理论为解决这一问题提供了严格的边界条件基础。

- **边界条件**：
  - 电位移矢量法向分量连续：$D_{1n} = D_{2n}$
  - 电场强度切向分量连续：$E_{1t} = E_{2t}$
  - 磁感应强度法向分量连续：$B_{1n} = B_{2n}$
  - 磁场强度切向分量连续：$H_{1t} = H_{2t}$

## 2.2 菲涅耳反射折射公式

为了处理矢量波的反射和折射，将入射光分解为两个互相垂直的振动分量：

- **s 分量**（垂直分量）：电矢量垂直于入射面。
- **p 分量**（平行分量）：电矢量平行于入射面。

**菲涅耳公式**描述了反射光、折射光与入射光振幅之间的关系：

- **反射系数**：
  - $\tilde{r}_s = \frac{\tilde{A}'_s}{\tilde{A}_s} = - \frac{\sin(i_1 - i_2)}{\sin(i_1 + i_2)}$
  - $\tilde{r}_p = \frac{\tilde{A}'_p}{\tilde{A}_p} = \frac{\tan(i_1 - i_2)}{\tan(i_1 + i_2)}$
- **透射（折射）系数**：
  - $\tilde{t}_s = \frac{\tilde{A}''_s}{\tilde{A}_s} = \frac{2 \sin i_2 \cos i_1}{\sin(i_1 + i_2)}$
  - $\tilde{t}_p = \frac{\tilde{A}''_p}{\tilde{A}_p} = \frac{2 \sin i_2 \cos i_1}{\sin(i_1 + i_2) \cos(i_1 - i_2)}$

其中 $i_1$ 为入射角，$i_2$ 为折射角，满足折射定律 $n_1 \sin i_1 = n_2 \sin i_2$。

## 2.3 反射率和透射率

- **光强反射率 ($R$)**：反射光强与入射光强之比。
  - $R_s = |\tilde{r}_s|^2 = \frac{\sin^2(i_1 - i_2)}{\sin^2(i_1 + i_2)}$
  - $R_p = |\tilde{r}_p|^2 = \frac{\tan^2(i_1 - i_2)}{\tan^2(i_1 + i_2)}$
  - 自然光入射时：$R = \frac{1}{2} (R_s + R_p)$
- **光强透射率 ($T$)**：透射光强与入射光强之比。
  - 注意：由于光强和折射率有关
  - $T = \frac{n_2 }{n_1 } |\tilde{t}|^2$
- 光功率透射率：
	- $\mathfrak{R_s}=R_S,\mathfrak{R_p}=R_p$
	- $\mathfrak{T_s}=\frac{cosi_2}{cosi_1}T_s$ (由于光束截面变化)
- **光功率透射率能量守恒**：$R + T = 1$（对无吸收介质）。
- 反射s分量大于p分量，透射p分量大于s分量

## 2.4 相位关系和半波损

- **半波损失（半波越变）**：
  - 当光从**光疏介质**射向**光密介质**（$n_1 < n_2$）时，反射光的相位发生 $\pi$ 的突变（即半个波长的损失）。
  - 具体表现：
    - 正入射时，反射光与入射光相位差 $\pi$。
    - 掠入射时，相位差趋于 $\pi$。
  - 当光从**光密介质**射向**光疏介质**（$n_1 > n_2$）时，反射光**没有**相位突变（在全反射角之前）。
- **斯托克斯（Stokes）倒逆关系**：利用光路可逆性推导出的反射与透射系数关系。
- 反射系数：$\tilde{r}_{21} = -\tilde{r}_{12}$ 
- 透射系数：$\tilde{t}_{12} \tilde{t}_{21} = 1 - \tilde{r}_{12}^2$

## 2.5 反射、折射时的偏振

- **布儒斯特角（Brewster Angle, $i_B$）**：
  - 当入射角满足 $\tan i_B = \frac{n_2}{n_1}$ 时（即 $i_1 + i_2 = 90^\circ$）。
  - **现象**：$R_p = 0$，即反射光中没有 p 分量，只有 s 分量。
  - **结论**：此时反射光为**完全线偏振光**（振动方向垂直于入射面）。
- **应用**：k
  - 制作偏振片（玻璃堆）。
  - 激光器中的布儒斯特窗（减少损耗，选偏振）。

## 2.6 全反射时的隐失波

- **全反射**：
  - 条件：光从光密介质射向光疏介质（$n_1 > n_2$），且入射角大于临界角 $i_c = \arcsin(n_2/n_1)$。
  - 现象：能量全部反射回第一介质，$R=1$。
- **隐失波（Evanescent Wave）**：
  - 在全反射情况下，第二介质中并非完全没有场，而是存在一种沿界面传播、向纵深方向指数衰减的波。
  - **特征**：
    - 非均匀平面波。
    - 沿界面方向（x 轴）传播。
    - 沿垂直界面方向（z 轴）振幅按指数规律衰减：$A(z) = A_0 e^{-z/d}$。
  - **穿透深度 ($d$)**：振幅衰减到 $1/e$ 的距离，通常在波长量级。
    - $d = \frac{\lambda_1}{2\pi \sqrt{\sin^2 i_1 - (n_2/n_1)^2}}$

## 2.7 近场扫描光学显微镜 (NSOM)

- **原理**：利用探针探测样品表面的隐失波。由于隐失波携带了样品表面的高频信息（细节），且被限制在表面极小范围内。
- **突破衍射极限**：传统显微镜分辨率受限于衍射极限（约 $\lambda/2$），而 NSOM 通过探测近场隐失波，分辨率取决于探针尖端的大小（可达纳米级）。
- **工作模式**：探针在距离样品表面几个纳米的范围内扫描。
